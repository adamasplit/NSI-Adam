class Maillon():
    def __init__(self, val, suiv):
        self.val = val
        self.suiv = suiv

    def __repr__(self):
        chaine = "val=" + str(self.val)
        return (chaine)

    def afficher(self):
        ch=str(self.valeur)
        if(self.suiv is not None):
            ch

n):


nombre=[5,7,56,75,80]
tmp=Maillon(5,Maillon(0,None))
lis=Maillon(10,tmp)
